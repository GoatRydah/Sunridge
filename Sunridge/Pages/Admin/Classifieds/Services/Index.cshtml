@page
@model Sunridge.Pages.Admin.Classifieds.IndexModel
@using Sunridge.Utility
@{
    ViewData["Title"] = "Classifieds Index";
}
@if (User.IsInRole(SD.AdminRole) || User.IsInRole(SD.OwnerRole))
{
    <h2>Admin Classified Services</h2>

    <div class="row">
        <div class="col-lg-3 col-md-6">
            <a asp-page="/Admin/Classifieds/Services/Upsert" class="btn btn-primary btn-sm">Add New Classified Service</a>
        </div>
        <div class="col-lg-3 col-md-6">
            <a asp-page="/Admin/Classifieds/Index" class="btn btn-primary btn-sm">Back to Classified Listings</a>
        </div>
    </div><div class="row">
        <div class="col-lg-3 col-md-6">
        </div>
    </div>
    <div class="table-responsive-sm">

        <table id="DT_Load" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th>Description</th>
                    <th></th>
                </tr>
            </thead>
        </table>
    </div>
}
@section Scripts {
    <script src="~/js/classifiedService.js"></script>
}